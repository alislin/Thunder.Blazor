@inherits StrapProgressBase
@if (IsVisabled)
{
    <div class="@CssStyle" style="@CssHeight">
        <div class="@CssProgress" style="width: @ProgressPercent">
            @if (ShowLabel)
            {
                @Caption
            }

            @if (ShowValue)
            {
                @ProgressPercent
            }
        </div>
    </div>
}