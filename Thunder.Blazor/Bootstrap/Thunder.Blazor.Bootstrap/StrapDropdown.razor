@inherits StrapDropdownBase
    <div class="@DropCss">
        @if (EnableDefaultMenuFlag)
        {
            var firstMenu = DataContext.ChildNodes.FirstOrDefault();
            <StrapButton Caption="@firstMenu.Caption" Style="@Style" Size="@Size" Outline="@Outline" CommandAction="@(()=> { OpenItemClick(firstMenu.CommandAction); })" ActionItemTag="@ActionItemTag" DisableButtonStyle="@DisableButtonStyle"></StrapButton>
            <StrapButton Style="@Style" Size="@Size" Outline="@Outline" StyleClass="@GetToggleCss(true)" CommandAction="@ToggleOpen" ActionItemTag="@ActionItemTag" DisableButtonStyle="@DisableButtonStyle"><span class="sr-only">Toggle Dropdown</span></StrapButton>
        }
        else
        {
            if (SubItem)
            {
                <StrapDropdownItem Caption="@Caption" CommandAction="@ToggleOpen" ActionItemTag="ActionItemTag.a" StyleClass="@GetToggleCss()"></StrapDropdownItem>
            }
            else
            {
                <StrapButton Caption="@Caption" StyleClass="@GetToggleCss()" CommandAction="@ToggleOpen" ActionItemTag="@ActionItemTag" DisableButtonStyle="@DisableButtonStyle"></StrapButton>
            }
        }
        @if (IsOpen)
        {
            <div class="@DropMenuCss">
                @if ((DataContext?.HasChildNodes ?? false))
                {
                    @foreach (var item in DataContext.ChildNodes)
                    {
                        if (item.HasChildNodes)
                        {
                            <StrapDropdown DataContext="@item" SubItem="true" DropDirection="@DropDirectionType.right" ActionItemTag="@ActionItemTag"></StrapDropdown>
                        }
                        else
                        {
                            <StrapDropdownItem Caption="@item.Caption" CommandAction="@(()=> { OpenItemClick(item.CommandAction); })" ActionItemTag="ActionItemTag.a"></StrapDropdownItem>
                        }
                    }
                }
                else
                {
                    @ChildContent
                }
            </div>
        }
    </div>
