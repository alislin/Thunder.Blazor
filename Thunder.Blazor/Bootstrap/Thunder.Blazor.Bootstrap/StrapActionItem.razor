@inherits StrapActionItemBase
@if (IsVisabled)
{
@if (!string.IsNullOrEmpty(Caption) && Caption.IndexOf("---") >= 0)
{
    //分割线
    <div class="dropdown-divider" @attributes="AdditionalAttributes"></div>
}
else
{
    @switch (ActionItemTag)
    {
        default:
        case ActionItemTag.button:
            <button class="@CssStyle" disabled="@(!IsEnabled)" @onclick="@CommandAction" @attributes="AdditionalAttributes">
                @if (ChildContent == null)
                {@Caption}
                else
                {@ChildContent}
            </button>
            break;
        case ActionItemTag.a:
            <a class="@CssStyle" href="javascript:;" @onclick="@CommandAction" @attributes="AdditionalAttributes">
                @if (ChildContent == null)
                {@Caption}
                else
                {@ChildContent}
            </a>
            break;
        case ActionItemTag.input:
            <input class="@CssStyle" type="button" value="@Caption" disabled="@(!IsEnabled)" @onclick="@CommandAction" @attributes="AdditionalAttributes"/>
            break;
        case ActionItemTag.submit:
            <input class="@CssStyle" type="submit" value="@Caption" disabled="@(!IsEnabled)" @onclick="@CommandAction" @attributes="AdditionalAttributes"/>
            break;
    }
}
}
